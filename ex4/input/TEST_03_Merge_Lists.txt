class IntList
{
    int head;
    IntList tail;
}

IntList MergeLists(IntList label1, IntList label2)
{
    if (label1 = nil)
    {
        return label2;
    }
    if (label2 = nil)
    {
        return label1;
    }
    
    if (label1.head < label2.head)
    {
        IntList l3 := label1;
        l3.tail := MergeLists(label1.tail,label2);
        return l3;
    }
    
    if (label2.head < label1.head)
    {
        IntList l3 := label2;
        l3.tail := MergeLists(label1,label2.tail);
        return l3;
    }
    
    /**************************************************/
    /* Execution falls off here ... what should we do */
    /**************************************************/
}

void main()
{
    IntList a := new IntList;
    IntList b := new IntList;
    IntList c := new IntList;
    IntList d := new IntList;

    IntList A := new IntList;
    IntList B := new IntList;
    IntList C := new IntList;
    IntList D := new IntList;
       
    a.head := 34;
    b.head := 70;
    c.head := 92;
    d.head := 96;

    A.head := 12;
    B.head := 50;
    C.head := 97;
    D.head := 99;

    a.tail := b;
    b.tail := c;
    c.tail := d;
    d.tail := nil;

    A.tail := B;
    B.tail := C;
    C.tail := D;
    D.tail := nil;
    
    IntList l := MergeLists(a,A);
    
    while (1 - (l = nil))
    {
        PrintInt(l.head);
        l := l.tail;
    }
}
